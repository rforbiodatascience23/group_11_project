```{r}
#| label: Loading libraries

library("tidyverse")
library("patchwork") #for plotting plots together (plo1/plot2)
library("ggridges")
#library("viridis")
library("dplyr")
library("table1") #describing a cohort
library("stringr") #count the number of occurrences of a given character in a string
library("ggseqlogo") #to create sequence logos
library("readr")
```

```{r}
prostate_data_clean <- prostate_data |>
  separate("rx", into = c("Treatment regime", "mg","Drug"), sep =" ") |>
  select(-c("mg"))


prostate_data_clean2 <- prostate_data_clean |>
  str_replace("Treatment regime", "placebo", "0") |>
  

    #mutate("Treatment regime" = case_when("Treatment regime"=="placebo" ~0, TRUE~ as.numeric(`Treatment regime`)))


 # mutate("Treatment regime" = case_when("Treatment regime" == "placebo" ~ "0"))

  
  #case_when(rx == "Placebo" ~ 0)
  #separate("rx", into = c("Treatment regime", "Drug"), sep =" ")
 
```
